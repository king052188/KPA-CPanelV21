var f = 0;
var time = 1000 * 10; // 10 sesonds

init_me();
// setInterval(init_me, time);

function init_me() {
    $(document).ready(function() {
        $.ajaxSetup({
            headers: {
                'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
            }
        });

        $.ajax({
            dataType: 'json',
            type:'POST',
            url: '/api/request/token',
            data: {app: appName, feed: f},
            beforeSend: function () {
                console.log(f + " Please wait...." + appName);
                f=1;
            }
        }).done(function(data){
            console.log(data);
            if(data.code > 200) {
                alert("Your session was expired!");
                window.location.href="/logout";
                return;
            }
        });
    })
}